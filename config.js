/* =====================================================================
   CONFIG — fill these in after you create your Supabase project.

   Dashboard -> Settings -> API Keys
     · If there is a key starting with  sb_publishable_...  use that one.
     · Otherwise open the "Legacy API Keys" tab and copy the  anon  key.
   Either works; the publishable key is the newer format and legacy anon
   keys are being retired at the end of 2026.

   Leave both empty and the app runs in local mode (no sync).
   This key is meant to be public; security comes from the row-level
   security policies in schema.sql.
   NEVER put a service_role or sb_secret_ key in this file.
   ===================================================================== */
const SUPABASE_URL = "https://wtnennlfmrjpvawvtdbv.supabase.co";
const SUPABASE_ANON_KEY = "sb_publishable_hEZCL3sjPlgK_rrsIj_-JA_aiDNq6wT";
